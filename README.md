# Form Builder using Javascript and working with the DOM

## Purpose
Parse through an array of data and append it to the DOM

## TODOS
- [ ] Iterate through the data and dynamically add the form fields to the page

## Challenges
